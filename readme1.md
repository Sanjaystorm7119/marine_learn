Admin dasboard flow :

1) Dashboard -> shows analtics charts
2) user management -> can change user role, add new user
3) user info -> 2 types 
                i) officers
                ii) crew
                Course assignment:
                Admin can assign the course to officer / crew based on courses available in Postgre table <course>, note: course table only has course name but not modules, so once a course is assigned , respective <study_modules> for course should be automatically assigned
4)role management -> create new role
5)course management
